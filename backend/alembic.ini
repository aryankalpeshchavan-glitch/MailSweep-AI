# Alembic configuration. The database URL is intentionally NOT stored here -
# alembic/env.py resolves it from application Settings (environment/.env),
# keeping one source of truth for configuration.

[alembic]
script_location = alembic
prepend_sys_path = .
sqlalchemy.url =

[alembic:exclude]
# nothing excluded
